// Конфигурация приложения
module.exports = {
  // Порт задаётся переменной окружения PORT (в Docker-контейнере — 5000),
  // по умолчанию — 3000 (локальная разработка и самопроверки).
  port: Number(process.env.PORT) || 3000
};